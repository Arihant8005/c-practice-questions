// Q. Change lowercase characters to uppercase 

#include<stdio.h>
#include<string.h>
int main()
{
    char str[50];
    printf("Enter the string:");
    scanf("%s",str);
    printf("%s",strupr(str));
    
}